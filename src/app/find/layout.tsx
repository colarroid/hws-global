import type { ReactNode } from "react";

/** The three questions share a progress bar and a single-column shell. */
export default function FindLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
